# PERF_ARCH_FRAMEWORK_SVG

**Location:** Ch.29 — The Performance Arc (manuscript line ~3714)

**What it needs:** A 4-layer architecture diagram showing the Performance Architecture Framework.

**Context from manuscript:**
> "Where the Neural Performance Model describes the arc of a show, the Performance Architecture Framework describes the invisible structure that makes each show possible to execute. Four layers. Each one is required before the next one is accessible."

The diagram should show four stacked layers where each layer is a prerequisite for the next. The surrounding text describes the layers — they need to be pulled from the chapter content to label the SVG correctly.

**Status:** SVG needs to be designed. Layer content is in the manuscript text following the placeholder.
